spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/banking_accounts_test

logging:
  level:
    com.banking: DEBUG
    org.springframework.data.mongodb: DEBUG

# Test-specific settings
management:
  endpoints:
    web:
      exposure:
        include: health,info
